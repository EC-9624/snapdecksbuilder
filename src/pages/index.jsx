import Card from "@/components/card";
import Header from "@/components/header";
import { useGetPosts } from "@/components/userRequests";
import Head from "next/head";

export default function Home() {
  const { cards, error } = useGetPosts();
  console.log(cards);

  // if (error) return <h1>Something went wrong!</h1>;
  // if (!cards) return <h1>Loading...</h1>;

  return (
    <div className=" bg-slate-800 min-w-full min-h-screen">
      <Head>
        <title>Marvel Snap Deckbuilder</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className="flex flex-wrap place-content-start  h-full w-full"></div>
    </div>
  );
}
